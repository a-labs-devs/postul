name: postul
description: "Aplicativo para encontrar postos de gasolina próximos com comparação de preços"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.1.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  http: ^1.1.0
  supabase_flutter: ^2.0.0
  google_maps_flutter: ^2.5.0
  latlong2: ^0.9.0
  geolocator: ^10.1.0
  intl: ^0.18.0
  shared_preferences: ^2.2.2
  flutter_polyline_points: ^2.0.1
  flutter_tts: ^3.8.5
  image_picker: ^1.0.7
  image: ^4.1.7
  path: ^1.8.3
  path_provider: ^2.1.2
  permission_handler: ^11.3.0
  photo_view: ^0.14.0
  provider: ^6.1.1
  
  # Notificações locais (sem Firebase!)
  flutter_local_notifications: ^17.2.3
  
  # 💰 Google AdMob - Monetização
  google_mobile_ads: ^5.1.0
  

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true